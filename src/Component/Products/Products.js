import React from 'react';

const Products = (props) => {
    console.log(props)
    const{productId,productName,price}= props.product;
    return (
        <div>
            
        </div>
    );
};

export default Products;